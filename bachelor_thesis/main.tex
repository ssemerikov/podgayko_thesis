% =============================================================
% bachelor_thesis/main.tex
% Кваліфікаційна робота бакалавра — Підгайко С.В.
% Структура за Положенням КДПУ 2025: титульний → запевнення →
% анотація → зміст → вступ → 2 розділи → висновки → список
% літератури → додатки.
% =============================================================

\documentclass[a4paper,14pt]{extarticle}

\usepackage{setup}

% Бібліографічний стиль: DSTU 8302:2015 («Бібліографічне посилання. Загальні
% положення та правила складання») формально відсутній у пакеті TeX Live 2021;
% biblatex-dstu не входить до дистрибутива. Використано gost-numeric як
% найближчий технічний еквівалент: алфавітне сортування, нумерована
% бібліографія, формат «Прізвище І.Б. Заголовок: тип. Місто: Видавництво,
% рік. Cтор. doi». Структурні відмінності DSTU 8302 vs gost-2008 (Russian
% origin, який є предком gost-numeric.bbx) є мінімальними і ручно
% коректуються в окремих записах. Альтернатива — встановлення biblatex-dstu
% з GitHub поза TeX Live 2021 — залишена як post-defense backlog.
\usepackage[
sorting=abyrvalg,
style=gost-numeric,
language=auto,
autolang=other,
backend=biber]{biblatex}

\DeclareSortingTemplate{abyrvalg}{
    \sort{\field{presort}}
    \sort[final]{\field{sortkey}}
    \sort{
        \field{sortname}
        \field{author}
        \field{editor}
        \field{sorttitle}
        \field{title}
        \field{journaltitle}
    }
    \sort{\field{sortyear}\field{year}}
    \sort{
        \field{sorttitle}
        \field{author}
        \field{editor}
        \field{title}
        \field{booktitle}
        \field{journaltitle}
    }
}

\usepackage{totcount}
\usepackage[figure,table]{totalcount}
\newtotcounter{citenum}
\AtEveryBibitem{\stepcounter{citenum}}

\addbibresource{scopus.bib}
\addbibresource{capstone.bib}

\newcommand*{\citet}[1]{\citeauthor{#1}~\cite{#1}}

% tikz уже завантажено через setup.sty; тут лише додаткові libraries.
\usetikzlibrary{shapes,arrows,positioning,chains,arrows.meta,fit,calc,backgrounds}

\pgfplotsset{compat=1.18}

% hyperref завантажується через setup.sty з опцією [unicode]; \hypersetup
% викликається там~же. Тут лише доналаштування для~цього документа.
\hypersetup{hidelinks}

\usepackage[normalem]{ulem}

\begin{document}

% --- Титульний аркуш (Додаток Б) ---
\input{chapters/title-page}

% --- Запевнення про доброчесність (Додаток В) ---
\input{chapters/integrity}

% --- Анотація (укр./англ.) ---
\input{chapters/summary}
\clearpage

% --- Зміст ---
\renewcommand{\contentsname}{\hspace*{\fill}\bfseries\Large ЗМІСТ\hspace*{\fill}}
\tableofcontents
\clearpage

% --- Вступ ---
\input{chapters/vstup}
\clearpage

% --- РОЗДІЛ 1 ---
\input{chapters/rozdil1}
\clearpage

% --- РОЗДІЛ 2 ---
\input{chapters/rozdil2}
\clearpage

% --- Загальні висновки ---
\input{chapters/vysnovky}
\clearpage

% --- Список використаної літератури ---
\printbibliography[heading=bibintoc,title={\hfil СПИСОК ВИКОРИСТАНИХ ДЖЕРЕЛ}]
\clearpage

% --- Додатки ---
\input{chapters/appendix}

\label{last_page}

\end{document}
